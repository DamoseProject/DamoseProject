package testing;

/**
 * Classe principale di test generata automaticamente per la verifica dell'ambiente.
 * Questa classe funge da entry point per test rapidi e dimostrazioni delle
 * funzionalità di base del linguaggio, come l'output formattato e le strutture
 * iterative (ciclo for).
 */
public class Main {

    /**
     * Metodo principale che avvia l'esecuzione del programma.
     * Stampa un messaggio di benvenuto e itera una sequenza numerica da 1 a 5,
     * permettendo di testare il debugger e i punti di interruzione (breakpoints).
     * * @param args Argomenti passati da riga di comando (non utilizzati in questo script).
     */
    public static void main(String[] args) {

        System.out.printf("Hello and welcome!");

        for (int i = 1; i <= 5; i++) {

            System.out.println("i = " + i);
        }
    }
}